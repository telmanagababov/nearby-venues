import { VenueComponent } from './venue.component';

describe('VenueComponent', () => {
  it('should be defined', () => {
    // Given
    const component = new VenueComponent();
    // When
    // Then
    expect(component).toBeDefined();
  });
});
